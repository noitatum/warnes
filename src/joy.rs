extern crate sdl2;

//nes use
use mem::Memory;
//sdl use
use sdl2::event::Event;
use sdl2::keyboard::Keycode;


// Reads the joystick (default to keyboard) and writes to memory accordingly.
pub fn read_joystick(mem: &mut Memory, pump: &mut sdl2::EventPump){

}
